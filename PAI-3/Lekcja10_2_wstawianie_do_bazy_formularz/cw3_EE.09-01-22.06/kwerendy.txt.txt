1. INSERT INTO odpowiedzi 
VALUES(NULL, 1, 5, "Przyda Ci się także wysoki stół, na którym postawisz pieska.")

2. SELECT nick, YEAR(CURRENT_DATE()) - rejestracja AS latNaForum
FROM konta
WHERE nick IN ('Jacek', 'Tomek', 'Monika');

3.SELECT nick, postow
FROM konta
INNER JOIN pytania ON konta.id = pytania.konta_id
WHERE pytania.id = 1

4.SELECT odpowiedzi.id, odpowiedz, konta.nick
FROM odpowiedzi
INNER JOIN konta ON odpowiedzi.konta_id = konta.id
WHERE Pytania_id = 1